"""Pure data shapes — no behavior, no dependencies on any other project
package. `ProjectContext` (context.py) and `Turn` (turn.py) are the two
things threaded through the agent's pipeline; the behavior that builds
and consumes them lives in agent/ instead.
"""
