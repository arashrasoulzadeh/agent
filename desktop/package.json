{
  "name": "agent-desktop",
  "version": "1.0.0",
  "description": "Cross-platform desktop client for the agent server — a generic renderer of the same server-driven UI the CLI (ui/app.py) renders, so every feature added server-side shows up in both.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
